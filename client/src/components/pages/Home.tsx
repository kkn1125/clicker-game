import Canvas from "@atoms/Canvas";
import { Stack, Typography } from "@mui/material";

const Home: React.FC = () => {
  return (
    <Stack flex={1}>
      <Canvas />
    </Stack>
  );
};

export default Home;
